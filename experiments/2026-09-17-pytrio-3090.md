# ReTool PyTRIO validation on 3090

Date: 2026-09-17. Repository commit used for training: `78e73a0`.

The `3090` host ran data preparation, the Python tool sandbox, and the PyTRIO client. Sampling and LoRA training ran on PyTRIO's remote service. The host has six RTX 3090 GPUs, but this run did not use them for model computation. Python 3.13.5 and the locked project dependencies were installed in an isolated virtual environment. The client reached PyTRIO through a temporary SSH reverse SOCKS proxy because direct outbound access from the host timed out.

## Data and configuration

- Training data: `BytedTsinghua-SIA/DAPO-Math-17k`, 1,791,650 train records after holding out 50 dev records.
- Evaluation data: `yentinglin/aime_2025` at revision `6f71d77b0b89b9dabe07ab466c51df33f514df7f`, 30 problems.
- Model: `Qwen/Qwen3.5-4B`; LoRA rank 32; 8 questions × 8 trajectories per step; trajectory cap 8,192 tokens; seed 42.
- The 20-step run used `--save-every 5` and `--swanlab-mode offline`. Offline SwanLab recording changes logging only.
- A 1-step, 2-question × 8-trajectory training smoke test completed before the 20-step run. A 1-problem × 1-sample AIME25 check confirmed the remote sampler and local code interpreter worked together.

## 20-step validation

The run completed in 1 h 09 min 53 s. Mean training `correct` was 0.359 for steps 1–10 and 0.428 for steps 11–20. Mean code calls were 1.691 and 1.600, respectively. These are training-batch means, not independent evaluation scores.

PyTRIO run: `run_n4rit1w1hqrc`. Step 5, 10, 15, and 20 checkpoints were saved. The final sampler weights are:

```text
trio://run_n4rit1w1hqrc/sampler_weights/retool-qwen35-4b-step20-final-weights
```

Both final checkpoints were also downloaded and validated as tar archives on the 3090 host:

| Local file | Contents | SHA-256 |
| --- | --- | --- |
| `/data/chenduo/retool/artifacts/retool-step20-final-weights.tar` | PEFT LoRA adapter | `b994742ab31bbf3d08bda132ac1143ea23b94132819a396b098e87163ec05aa9` |
| `/data/chenduo/retool/artifacts/retool-step20-final-state.tar` | LoRA adapter and optimizer state | `3cc1ccb2db079da536bbb8bd474a70d2fead3457564ba45a98ec2c8d6995cc27` |

PyTRIO's download guide describes a zip file, but the service returned valid tar archives for these checkpoints. The archives are kept outside Git because they are large binary artifacts.

## AIME25 evaluation

Both evaluations used `--mode retool --val-n 12 --temperature 1.0 --top-p 0.7`.

| Model | Completed problems | Correct generations | Average@12 | Pass@12 | Format |
| --- | ---: | ---: | ---: | ---: | ---: |
| Base | 30/30 | 94/360 | 26.11% | 53.33% | 28.33% |
| Step 20 | 10/30 | 71/120 | incomplete | incomplete | incomplete |

On the same first ten problems, Base had 50/120 correct generations and Step 20 had 71/120. This subset is not representative of the complete benchmark.

Step 20 evaluation stopped during problem 11–15 when PyTRIO returned a 409 conflict while renewing a sampling run. A subsequent resume attempt returned `billing_insufficient_balance`; no further paid work was submitted. The completed first ten problems remain in `/data/chenduo/retool/05-retool/eval-results/aime25-retool-step20.jsonl` on the host. Full Base results are in the adjacent `aime25-retool-base.jsonl`.

After the workspace balance is restored and a stable outbound route to PyTRIO is available, resume the Step 20 evaluation from problem index 10 using the existing output file and the `--start-index 10` option added in commit `5edd16c`. Do not start a 200-step run until the complete Step 20 evaluation and billing state have been checked.
